export interface IAuthFormInput {
  type: string;
  placeholder?: string;
  name: string;
  id: string;
  extraStyle?: string;
  maxLength?: number;
  accept?:string,
  error?: string;
  togglePasswordExtraStyle?: string;
}

// mitchimage
